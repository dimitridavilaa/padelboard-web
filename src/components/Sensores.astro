---
// src/components/Sensores.astro
---

<section id="sensores" class="sensores-section">
    
    <div class="container fade-up relative-container">
        
        <h2 class="main-title">SENSORES</h2>

        <div class="blue-block-middle">
            
            <div class="sensores-grid">
                
                <div class="side-col">
                    <div class="sensor-card">
                        <img src="/img/sensor_verde.jpg" alt="Sensor Suma" />
                    </div>
                    <p class="sensor-text">Presiona el sensor 1 segundo y se ilumina en verde</p>
                </div>

                <div class="center-col">
                    <p class="main-desc">
                        Los sensores PadelBoard son dispositivos alámbricos diseñados para pádel. Cada sensor detecta la pulsación del jugador garantizando una respuesta inmediata, sin retrasos y sin interferencias.
                    </p>
                    
                    <h3 class="benefits-title">BENEFICIOS</h3>
                    
                    <ul class="benefits-list">
                        <li>No existe el riesgo de que se apaguen en medio del partido.</li>
                        <li>No necesitan cargarse nunca.</li>
                        <li>La instalación se hace una sola vez.</li>
                        <li>No se deteriora con el tiempo.</li>
                    </ul>
                </div>

                <div class="side-col">
                    <div class="sensor-card">
                        <img src="/img/sensor_rojo.jpg" alt="Sensor Resta" />
                    </div>
                    <p class="sensor-text">Presiona el sensor 3 segundos y se ilumina en rojo</p>
                </div>

            </div>
        </div>

        <div class="bottom-titles">
            <h3 class="action-title">SUMA TUS PUNTOS</h3>
            <h3 class="action-title">RESTA TUS PUNTOS</h3>
        </div>

    </div>

</section>

<style>
    /* --- SECCIÓN PRINCIPAL --- */
    .sensores-section {
        position: relative;
        width: 100%;
        min-height: 750px;
        
        /* IMAGEN DE FONDO */
        background-image: url('/img/sensores_bg.jpg'); 
        background-size: cover;
        background-position: center;
        
        display: flex;
        align-items: center;
        justify-content: center;
        overflow: hidden;
    }
  
    .relative-container {
        position: relative;
        width: 100%;
        height: 100%;
        display: flex;
        flex-direction: column;
        justify-content: center; /* Centramos verticalmente el conjunto */
        padding-top: 100px; 
        padding-bottom: 60px;
    }
  
    /* --- TÍTULO SUPERIOR --- */
    .main-title {
        font-family: var(--fuente-titulos); /* Anton */
        font-size: 6rem;
        color: #FFDE59;
        text-align: center;
        text-transform: uppercase;
        
        /* CAMBIO: Más espacio abajo para empujar el bloque azul */
        margin-bottom: 140px; 
        
        text-shadow: 0 10px 30px rgba(0,0,0,0.8);
        position: relative;
        z-index: 2;

        transform: translateY(-30px);
    }
  
    /* --- FRANJA AZUL CENTRAL --- */
    .blue-block-middle {
        position: relative;
        /* CAMBIO: Ya no usamos 100vw aquí para el contenido */
        width: 100%; 
        max-width: 1200px; /* Igual que abajo */
        margin: 0 auto; /* Centrado igual que abajo */
        
        padding: 25px 0; 
        
        /* Quitamos el background-color de aquí y lo pasamos al ::before */
    }

    /* ESTO CREA EL FONDO AZUL INFINITO DETRÁS */
    .blue-block-middle::before {
        content: '';
        position: absolute;
        top: 0; 
        bottom: 0;
        left: 50%; /* Punto de partida: centro */
        width: 100vw; /* Ancho total de la pantalla */
        transform: translateX(-50%); /* Lo centramos a la fuerza */
        
        background-color: rgba(0, 0, 0, 0.6); /* rgba(4, 19, 62, 0.7); */
        backdrop-filter: blur(2px);
        z-index: -1; /* Se pone detrás del contenido */
    }
  
    /* GRID DEL CONTENIDO */
    .sensores-grid {
        width: 100%;
        max-width: 1200px;
        padding: 0 20px;
        display: grid;
        /* Ajuste de columnas: Centro más ancho para el texto */
        grid-template-columns: 1fr 2fr 1fr; 
        gap: 20px;
        align-items: center;
    }
  
    /* --- COLUMNAS LATERALES (Para centrar las fotos pequeñas) --- */
    .side-col {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
    }
  
    /* --- IMÁGENES DE SENSORES --- */
    .sensor-card {
        /* CAMBIO: Tamaño fijo mucho más pequeño */
        width: 220px; 
        height: 220px; /* Cuadrado perfecto */
        
        border-radius: 20px;
        overflow: hidden;
        box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        margin-bottom: 15px;
    }
  
    .sensor-card img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }
  
    .sensor-text {
        font-family: var(--fuente-cuerpo);
        font-size: 0.9rem; /* Texto descriptivo pequeño */
        font-weight: bold;
        color: white;
        text-align: center;
        line-height: 1.3;
        max-width: 200px; /* Que no se ensanche más que la foto */
    }
  
    /* TEXTO CENTRAL */
    .center-col {
        text-align: center;
        color: white;
        padding: 0 40px; /* Aire a los lados del texto */
    }
  
    .main-desc {
        font-size: 1.1rem;
        line-height: 1.5;
        margin-bottom: 20px;
    }
  
    .benefits-title {
        font-family: var(--fuente-titulos);
        color: #FFDE59;
        font-size: 2rem;
        margin-bottom: 15px;
        text-transform: uppercase;
    }
  
    .benefits-list {
        list-style: none;
        padding: 0;
        margin: 0;
    }
  
    .benefits-list li {
        font-family: var(--fuente-cuerpo);
        font-size: 1rem;
        margin-bottom: 8px;
        opacity: 0.9;
    }
  
    /* --- TÍTULOS INFERIORES --- */
    .bottom-titles {
        margin-top: 15px; 
        display: grid;
        grid-template-columns: 1fr 2fr 1fr;
        gap: 20px;
        width: 100%;
        max-width: 1200px;
        padding: 0 20px;
        margin-left: auto;
        margin-right: auto;
    }

    .action-title:last-child {
      grid-column: 3;
    }
  
    .action-title {
        font-family: var(--fuente-titulos);
        color: #FFDE59;
        font-size: 2rem;
        text-transform: uppercase;
        text-shadow: 0 5px 10px rgba(0,0,0,0.8);
        margin: 0;
        width: 100%;
        display: flex;
        justify-content: center; 
        text-align: center;
    }
  
    /* --- RESPONSIVE --- */
    @media (max-width: 1024px) {
        .main-title { font-size: 4rem; margin-bottom: 40px;}
        .sensores-grid { grid-template-columns: 1fr; gap: 40px; }
        
        /* En tablet/móvil cambiamos el orden */
        .center-col { order: -1; padding-bottom: 20px; border-bottom: 1px solid rgba(255,255,255,0.1); margin-bottom: 20px;}
        
        .bottom-titles {
            flex-direction: column;
            gap: 20px;
            align-items: center;
            margin-top: 40px;
        }
        
        .action-title { width: auto; }
    }
  </style>