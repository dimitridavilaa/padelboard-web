---
// src/components/USP.astro
---
<section id=usp class="usp-section">
  <div class="container fade-up">
    <div class="texto-wrapper">
      <h2 class="h2-titulo">THE ONLY SOLUTION DESIGNED, MANUFACTURED, AND SUPPORTED IN THE AMERICAS.</h2>
      <p>PadelBoard was born in the Americas, for the clubs of the Americas.</p>
      <p>With real-time support, local infrastructure, and a product built for the continent's demands, we guarantee superior performance, reliability, and a professional experience for both clubs and players.</p>
      <p>We offer proximity, immediate attention, and a solution built to last.</p>
    </div>
    
    <div class="mapa-container">
      <img src="/img/mapa-america.png" alt="Map of the american continent" class="mapa-img" />
      
      <div class="marcador-wrapper">
          <div class="pulso"></div>
          <div class="punto"></div>
      </div>
    </div>
  </div>
</section>

<style>
  .usp-section {
    padding: 45px 1px;
    background: var(--gradiente-padel);
    color: white;
    overflow: hidden;
  }
  
  .container {
    max-width: 1100px; margin: 0 auto;
    display: grid; grid-template-columns: 1fr 1fr; gap: 40px; align-items: center;
  }
  
  .h2-titulo { font-size: 2rem; margin-bottom: 44px; color: #fff; line-height: 50px; }
  p { font-size: 1.2rem; color: white; margin-bottom: 20px; }
  
  /* --- CONTENEDOR DEL MAPA --- */
  .mapa-container {
    position: relative; /* Clave para poder posicionar el punto encima */
    width: 100%;
    max-width: 450px; /* Tamaño del mapa */
    margin: 0 auto;
    display: flex;
    justify-content: center;
  }

  .mapa-img {
    width: 100%;
    height: auto;
    opacity: 0.6;
    
    /* TRUCO HOLOGRAMA: 
       2. drop-shadow -> Crea el resplandor de neón azulado
    */
    filter: invert(1) brightness(2) /* drop-shadow(0 0 15px rgba(59, 130, 246, 0.8)); */
  }

  /* --- MARCADOR DE UBICACIÓN --- */
  .marcador-wrapper {
    position: absolute;
    /* --- ¡AJUSTA ESTAS COORDENADAS! --- */
    top: 64%;  /* Mueve arriba/abajo */
    left: 64%; /* Mueve izquierda/derecha */
    /* ----------------------------------- */
    transform: translate(-50%, -50%);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .punto {
    width: 16px; height: 16px;
    background-color: var(--color-amarillo); /* Punto amarillo */
    border-radius: 50%;
    z-index: 2;
    box-shadow: 0 0 10px rgba(246, 206, 20, 0.8);
  }

  .pulso {
    position: absolute;
    width: 60px; height: 60px;
    background-color: var(--color-azul-claro);
    border-radius: 50%;
    z-index: 1;
    opacity: 0.6;
    animation: latido 2s infinite ease-out;
  }

  @keyframes latido {
    0% { transform: scale(0.5); opacity: 0.8; }
    100% { transform: scale(1.5); opacity: 0; }
  }

  @media (max-width: 768px) {
      .container { 
          grid-template-columns: 1fr; 
          text-align: center;
          gap: 10px; /* Reducimos el gap general */
      }

      /* TRUCO PRO: "Deshacemos" la caja contenedora del texto para poder 
         mover sus hijos (h2 y p) individualmente */
      .texto-wrapper {
          display: contents; 
      }
      
      /* 1. EL TÍTULO VA PRIMERO */
      .h2-titulo {
          order: 1;
          font-size: 1.9rem; /* Ajuste para que no sea gigante en móvil */
          line-height: 1.2;
      }

      /* 2. EL MAPA VA EN EL MEDIO */
      .mapa-container { 
          order: 2;
          margin: 10px auto; 
          width: 80%; /* Un poco más compacto */
          margin-bottom: 20px;
      } 

      /* 3. LOS PÁRRAFOS VAN AL FINAL */
      .texto-wrapper p {
          order: 3;
          margin-bottom: 0;
      }
  }
</style>